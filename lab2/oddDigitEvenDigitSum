# Program to find the sum of digits at even places and odd places (from right)

number = int(input("Enter a number: "))
odd_sum = 0
even_sum = 0
place = 1  # Start from rightmost digit (place 1)

while number > 0:
    digit = number % 10
    if place % 2 == 1:  # Odd place
        odd_sum += digit
    else:  # Even place
        even_sum += digit
    number //= 10
    place += 1

print("Sum of digits at odd places:", odd_sum)
print("Sum of digits at even places:", even_sum)    